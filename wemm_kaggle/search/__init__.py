"""v0.3.0-rc1 localhost semantic search subsystem (Qdrant + Wikidata EN-VI).

This package is a separate integration layer on top of the frozen v0.2.0
embedding API. It never modifies the frozen `wemm_kaggle/api` package.
"""
